/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['github.com', 'images.unsplash.com'],
  },
  experimental: {
    //appDir: true,
  },
}

module.exports = nextConfig
